{
  "name": "@mokronos/gateway-api",
  "version": "0.2.0",
  "type": "module",
  "description": "Gateway HTTP API definitions, handlers, and server assembly.",
  "license": "MIT",
  "publishConfig": { "access": "public", "registry": "https://registry.npmjs.org" },
  "files": ["dist", "README.md"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "bun": "./src/index.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@mokronos/contracts": "workspace:*",
    "@mokronos/gateway-core": "workspace:*",
    "@mokronos/integrations": "workspace:*",
    "@mokronos/integrations-client": "workspace:*",
    "@mokronos/observability": "workspace:*",
    "effect": "catalog:",
    "@effect/platform-bun": "catalog:"
  }
}
