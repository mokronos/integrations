{
  "name": "@mokronos/gateway-core",
  "version": "0.2.0",
  "type": "module",
  "description": "Gateway domain, persistence, authorization, and execution policy.",
  "license": "MIT",
  "publishConfig": { "access": "public", "registry": "https://registry.npmjs.org" },
  "files": ["dist", "README.md"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "bun": "./src/index.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./domain": {
      "types": "./dist/domain.d.ts",
      "bun": "./src/domain.ts",
      "development": "./src/domain.ts",
      "default": "./dist/domain.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@libsql/client": "catalog:",
    "@mokronos/contracts": "workspace:*",
    "@mokronos/integrations": "workspace:*",
    "@mokronos/integrations-client": "workspace:*",
    "effect": "catalog:"
  }
}
