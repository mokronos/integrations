# @mokronos/integrations-protocol

Dependency-light Effect Schema contracts shared across the integrations wire
boundary. The gateway, TypeScript client, and Executor derive their runtime
decoders and TypeScript types from these definitions.

This package contains data contracts only. It performs no HTTP requests,
storage, discovery, authorization, or execution.
