{
  "name": "@mokronos/integrations-platform-demo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A minimal application that embeds the gateway core in-process: its own database, its own migrations, one page.",
  "license": "MIT",
  "engines": {
    "bun": ">=1.2"
  },
  "scripts": {
    "dev": "bun run src/main.ts",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@effect/platform-bun": "catalog:",
    "@effect/sql-libsql": "catalog:",
    "@mokronos/integrations-contracts": "workspace:*",
    "@mokronos/integrations-gateway-core": "workspace:*",
    "@mokronos/integrations-host": "workspace:*",
    "@libsql/client": "catalog:",
    "drizzle-orm": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "drizzle-kit": "catalog:"
  }
}
